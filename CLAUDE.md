# Project: so

Scott's orchestrator — a small CLI for managing agent sessions (Claude Code, cursor-agent, etc.) in a single tmux session and feeding them prompts.

## Git / PR rules for this project

**No `Co-Authored-By` trailers on commits in this repo.** Personal project — overrides the work-overlay rule at `~/.claude/CLAUDE.md` that requires co-author trailers for AI-authored work.

**No "🤖 Generated with Claude Code" footer on PR bodies in this repo.** Same reasoning.

## Design

See `docs/superpowers/specs/` for the v0 design spec. Read that first before making non-trivial changes.
