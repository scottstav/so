# so

Scott's orchestrator. A small CLI for managing agent sessions (Claude Code, cursor-agent, etc.) in a single tmux session, with a primitive for feeding prompts into running agents.

Design: see `docs/superpowers/specs/`.

Status: early. v0 in progress.
